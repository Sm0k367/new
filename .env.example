# .env.example - Copy this to .env and fill in your real values

# Required: Get your API key from https://console.x.ai/
GROK_API_KEY=your_xai_grok_api_key_here

# Optional: Change the port if 3000 is in use locally
PORT=3000

# Optional: In production (e.g. Render/Railway), you might want to restrict origins
# Allowed origins for Socket.io CORS (comma-separated or single URL)
# CORS_ORIGIN=https://your-vercel-app-name.vercel.app
CORS_ORIGIN=*
